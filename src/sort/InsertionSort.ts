import { Sort } from "./Sort";

export class InsertionSort implements Sort {

    sort(nums: number[]): number[] {
        let sortedNums: number[] = [1, 2, 3, 4];
        console.log('sort with insertion sort');
        return sortedNums;
    }

}
