import { Sort } from "./Sort";

export class BubbleSort implements Sort {

    sort(nums: number[]): number[] {
        console.log('sorting with Bubble sort');
        let sortedNumbers: number[] = [1, 2, 3];
        return sortedNumbers;
    }

}
